name: create_pr_main_to_develop
on:
  push:
    branches:
      - main

jobs:
  create_pr:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ secrets.AUTOMATION_PAT }}

      - name: Set current datetime as env variable
        env:
          TZ: 'Asia/Tokyo'
        run: echo "CURRENT_DATETIME=$(date +'%Y%m%d%H%M%S')" >> $GITHUB_ENV

      - name: Create Branch
        run: git checkout -b merge/gha_${{ env.CURRENT_DATETIME }}

      - name: Push Branch
        run: git push origin merge/gha_${{ env.CURRENT_DATETIME }}

      - name: Create Pull Request
        env:
          GH_TOKEN: ${{ secrets.AUTOMATION_PAT }}
          BRANCH_NAME: merge/gha_${{ env.CURRENT_DATETIME }}
        run: |
          gh pr create --title "[bot(create_pr_main_to_develop)]: created at ${{ env.CURRENT_DATETIME }}" --body "[bot(create_pr_main_to_develop)]: created at ${{ env.CURRENT_DATETIME }}" --base develop --head $BRANCH_NAME
